<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>cTMT Cognitive Recon Mission</title>
<style>
  :root{
    --bg:#07111f; --panel:#0d1b2a; --panel2:#102536; --text:#ecf7ff; --muted:#91a9ba;
    --line:#244154; --accent:#1fd7c5; --accent2:#73ffd2; --danger:#ff7b72; --stage:#f7fbfd;
  }
  *{box-sizing:border-box}
  html,body{width:100%;max-width:100%;overflow-x:hidden}
  body{margin:0;font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:var(--bg);color:var(--text)}
  .wrap{width:100%;max-width:100%;margin:0;padding:16px 14px 20px;background:radial-gradient(circle at 75% 0%,#12334a 0,transparent 34%),var(--bg)}
  .top{display:flex;gap:16px;align-items:center;justify-content:space-between;margin-bottom:12px;min-width:0}
  h1{font-size:21px;margin:0;letter-spacing:.02em}
  .kicker{font-size:10px;font-weight:900;letter-spacing:.16em;color:var(--accent2);margin-bottom:4px;text-transform:uppercase}
  .pill{background:#102d39;color:var(--accent2);border:1px solid #245766;border-radius:999px;padding:7px 11px;font-size:11px;font-weight:900;letter-spacing:.08em}
  .operator{display:flex;gap:11px;align-items:center;border:1px solid #245766;background:linear-gradient(90deg,#0b1b29,#102b38);border-radius:14px;padding:10px 12px;margin-bottom:12px}
  .operator-avatar{width:38px;height:38px;display:flex;align-items:center;justify-content:center;border-radius:50%;background:#07111f;border:1px solid #32677a;font-size:21px;flex:0 0 38px}
  .operator-name{font-size:9px;font-weight:900;letter-spacing:.13em;color:var(--accent2);text-transform:uppercase}
  .operator-msg{font-size:12px;color:#c7d9e3;line-height:1.45;margin-top:2px}
  .panel{background:rgba(13,27,42,.96);border:1px solid var(--line);border-radius:14px;padding:14px;min-width:0;box-shadow:0 10px 30px rgba(0,0,0,.12)}
  .grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(220px,260px);gap:12px;width:100%}
  .grid>.panel{min-width:0;overflow:hidden}
  .controls{display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin-bottom:12px;background:#0a1724}
  button{border:1px solid transparent;border-radius:8px;padding:9px 13px;font-weight:850;cursor:pointer;letter-spacing:.02em}
  button.primary{background:var(--accent);color:#05211e}
  button.secondary{background:#153043;color:#d8e7ee;border-color:#27495b}
  button.danger{background:#351e25;color:#ffb4ae;border-color:#62313a}
  button:disabled{opacity:.4;cursor:not-allowed}
  .hud-title{display:flex;justify-content:space-between;gap:12px;align-items:end;margin-bottom:7px}
  #trialLabel{font-weight:900;letter-spacing:.035em;color:#f5fbff}
  #status{font-weight:900;letter-spacing:.07em;text-transform:uppercase}
  #stage{
    position:relative;width:100%;aspect-ratio:10/7;min-height:480px;
    background:linear-gradient(rgba(21,84,105,.035) 1px,transparent 1px),linear-gradient(90deg,rgba(21,84,105,.035) 1px,transparent 1px),var(--stage);
    background-size:26px 26px;border:1px solid #3f6374;border-radius:10px;
    overflow:hidden;touch-action:none;user-select:none;cursor:crosshair;box-shadow:inset 0 0 0 1px rgba(31,215,197,.08)
  }
  #stage:before{content:"TRACKING FIELD";position:absolute;left:10px;top:8px;font-size:8px;font-weight:900;letter-spacing:.14em;color:#73909f;z-index:0;pointer-events:none}
  .target{
    position:absolute;width:44px;height:44px;border-radius:50%;transform:translate(-50%,-50%);
    display:flex;align-items:center;justify-content:center;background:white;border:3px solid #111827;
    color:#111827;font-size:15px;font-weight:800;z-index:3;pointer-events:none;
  }
  .target.hit{border-color:#0f9f91;background:#e7fff8;color:#065f56;box-shadow:0 0 0 4px rgba(31,215,197,.12)}
  .fixation{
    position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);
    width:76px;height:76px;border-radius:50%;background:#0c2431;border:2px solid var(--accent);
    color:var(--accent2);display:flex;align-items:center;justify-content:center;font-size:36px;font-weight:400;z-index:5;
    cursor:pointer;box-shadow:0 0 0 8px rgba(31,215,197,.08),0 0 24px rgba(31,215,197,.22);
  }
  .fixation:after{content:"INITIALIZE";position:absolute;top:86px;left:50%;transform:translateX(-50%);font-size:9px;font-weight:900;letter-spacing:.14em;color:#276274;white-space:nowrap}
  svg.trace{position:absolute;inset:0;width:100%;height:100%;z-index:1;pointer-events:none}
  .crosshair{position:absolute;width:7px;height:7px;border-radius:50%;background:#087f72;opacity:.8;transform:translate(-50%,-50%);z-index:2;pointer-events:none}
  .stats{display:grid;gap:9px;min-width:0}
  .stat{padding:11px;border:1px solid #244154;border-radius:10px;min-width:0;background:#0a1724}
  .stat b{display:block;font-size:21px;margin-top:3px;color:#f7fcff}
  .small{font-size:11px;color:var(--muted)}
  .note{font-size:12px;line-height:1.55;color:#a9bfcc;overflow-wrap:anywhere}
  .progress{height:7px;background:#132c3c;border-radius:999px;overflow:hidden;margin:8px 0 10px;border:1px solid #213e50}
  .progress>div{height:100%;background:linear-gradient(90deg,var(--accent),var(--accent2));width:0%;box-shadow:0 0 14px rgba(31,215,197,.45)}
  .ok{color:var(--accent2)!important}.warn{color:#ffbd75!important}
  .hidden{display:none!important}
  pre{white-space:pre-wrap;word-break:break-word;max-height:230px;overflow:auto;background:#050b12;color:#9cc9d5;padding:11px;border-radius:10px;font-size:10px;border:1px solid #1e3544}
  hr{border-color:#244154!important}
  @media (max-width:760px){
    .grid{grid-template-columns:1fr}
    .top{flex-wrap:wrap}
    #stage{min-height:380px}
  }
</style>
</head>
<body>
<div class="wrap">
  <div class="top">
    <div>
      <div class="kicker">Cognitive Recon // Mission Control</div>
      <h1>cTMT TRACKING MISSION</h1>
      <div class="small">Protocol locked · Training A/B + 20 alternating trials · 25 s per trial</div>
    </div>
    <div class="pill">● TRACKING ONLINE</div>
  </div>

  <div class="operator">
    <div class="operator-avatar">🧑‍🚀</div>
    <div><div class="operator-name">Mission Operator // Echo</div><div class="operator-msg">중앙 ⊕로 tracking을 초기화한 뒤 목표를 자연스럽게 따라가세요. 사격·WASD·시점 조작은 없습니다.</div></div>
  </div>

  <div class="panel controls">
    <button id="startBtn" class="primary">MISSION START</button>
    <button id="nextBtn" class="secondary" disabled>NEXT MISSION</button>
    <button id="resetBtn" class="danger">RESET</button>
    <button id="downloadJsonBtn" class="secondary" disabled>RAW JSON</button>
    <button id="sendPythonBtn" class="primary" disabled>ANALYZE</button>
  </div>

  <div class="grid">
    <div class="panel">
      <div class="hud-title">
        <div>
          <div id="trialLabel">MISSION STANDBY</div>
          <div id="instruction" class="small">MISSION START를 눌러주세요.</div>
        </div>
        <div id="status" class="small">READY</div>
      </div>
      <div class="progress"><div id="progressBar"></div></div>
      <div id="stage" aria-label="cTMT test area" tabindex="0"></div>
    </div>

    <div class="panel">
      <div class="kicker">LIVE HUD</div>
      <div class="stats">
        <div class="stat"><span class="small">NEXT OBJECTIVE</span><b id="nextTarget">-</b></div>
        <div class="stat"><span class="small">TARGETS SECURED</span><b id="correctCount">0 / 15</b></div>
        <div class="stat"><span class="small">TIME REMAINING</span><b id="remaining">25.00 s</b></div>
        <div class="stat"><span class="small">TRACKING EVENTS</span><b id="eventCount">0</b></div>
      </div>
      <hr style="border:0;border-top:1px solid var(--line);margin:14px 0">
      <div class="note">중앙 <b>⊕</b>를 클릭하면 trial이 시작됩니다. 이후 클릭하지 말고 마우스 커서를 순서대로 원 안에 통과시키세요. 15개를 모두 연결하거나 25초가 지나면 종료됩니다.</div>
      <div id="summaryBox" class="hidden">
        <hr style="border:0;border-top:1px solid var(--line);margin:14px 0">
        <b style="color:var(--accent2)">ALL MISSIONS COMPLETE</b>
        <div class="small" id="summaryText"></div>
      </div>
    </div>
  </div>

  <div class="panel" style="margin-top:12px">
    <div class="kicker" style="margin-bottom:7px">SYSTEM LOG // RECENT TRIAL</div>
    <pre id="preview">NO TRACKING DATA</pre>
  </div>
</div>

<script>
function sendMessageToStreamlitClient(type, data) {
  const outData = Object.assign({isStreamlitMessage:true,type:type}, data);
  window.parent.postMessage(outData, "*");
}
function initStreamlitComponent() {
  sendMessageToStreamlitClient("streamlit:componentReady", {apiVersion:1});
}
function setStreamlitFrameHeight(height) {
  sendMessageToStreamlitClient("streamlit:setFrameHeight", {height:height});
}
function sendDataToPython(value) {
  sendMessageToStreamlitClient("streamlit:setComponentValue", {value:value});
}
window.addEvent